server:
  port: 8081

spring:
  application:
    name: it-gateway

eureka:
  client:
#    registerWithEureka: false
#    fetchRegistry: false
    serviceUrl:
      defaultZone: http://localhost:8880/eureka

zuul:
  routes:
    it-service: /it-service/**
    it-ip: /ip/**

ribbon:
  readTimeout: 5000
  connectTimeout: 5000